# HADDOCK Benchmarking Suite

This repository contains the benchmarking suite for the HADDOCK software suite.

## Setup

Requires Anaconda or Miniconda to be installed and `conda` to be in your `PATH`.

Setup the python environment and install HADDOCK3.

```bash
bash setup.sh
```

## Usage
